interface:
  display_name: "Setup Dev Env"
  short_description: "Audit and prepare layered local toolchains"
  icon_small: "./assets/icon.png"
  icon_large: "./assets/icon.png"
  brand_color: "#0F766E"
  default_prompt: "Use $setup-dev-env to audit my local toolchains and preserve each tool's declared manager ownership."
policy:
  allow_implicit_invocation: true

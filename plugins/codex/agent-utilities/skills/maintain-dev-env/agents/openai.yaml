interface:
  display_name: "Maintain Dev Env"
  short_description: "Diagnose layered local developer toolchains"
  icon_small: "./assets/icon.png"
  icon_large: "./assets/icon.png"
  brand_color: "#0F766E"
  default_prompt: "Use $maintain-dev-env to diagnose missing or duplicate commands, nested manager ownership, Node npm globals, and project toolchain mismatches."
policy:
  allow_implicit_invocation: true

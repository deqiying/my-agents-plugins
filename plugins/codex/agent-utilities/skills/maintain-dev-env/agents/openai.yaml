interface:
  display_name: "Maintain Dev Env"
  short_description: "Maintain Java, Maven, mvnd, and local toolchains"
  icon_small: "./assets/icon.png"
  icon_large: "./assets/icon.png"
  brand_color: "#0F766E"
  default_prompt: "Use $maintain-dev-env to audit or repair my Java, Maven, mvnd, or other local toolchain and load only the relevant bundled manager workflow."
policy:
  allow_implicit_invocation: true

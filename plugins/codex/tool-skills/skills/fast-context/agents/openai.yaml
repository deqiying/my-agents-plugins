interface:
  display_name: "Fast Context"
  short_description: "Locate relevant local code with semantic search"
  icon_small: "./assets/icon.png"
  icon_large: "./assets/icon.png"
  brand_color: "#0F766E"
  default_prompt: "Use $fast-context as the host entry for matching local-code semantic discovery unless external transmission is prohibited, load the installed CLI's version-matched Skill, then verify every candidate deterministically."

policy:
  allow_implicit_invocation: true

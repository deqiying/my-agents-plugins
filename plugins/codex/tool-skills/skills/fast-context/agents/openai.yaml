interface:
  display_name: "Fast Context"
  short_description: "Locate relevant local code with semantic search"
  icon_small: "./assets/icon.png"
  icon_large: "./assets/icon.png"
  brand_color: "#0F766E"
  default_prompt: "Use $fast-context to locate the implementation behind this request, then verify the returned code locally."

policy:
  allow_implicit_invocation: true

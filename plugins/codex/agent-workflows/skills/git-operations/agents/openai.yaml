interface:
  display_name: "Git Operations"
  short_description: "Safe Git operations with conditional staging"
  icon_small: "./assets/icon.png"
  icon_large: "./assets/icon.png"
  brand_color: "#16A34A"
  default_prompt: "Use $git-operations to inspect staged and unstaged changes separately, stage only missing task changes, and perform approved Git operations safely."

policy:
  allow_implicit_invocation: true

interface:
  display_name: "证据驱动诊断"
  short_description: "定位重复失败、证据冲突与多层状态不一致的诊断处置流程"
  icon_small: "./assets/icon.png"
  icon_large: "./assets/icon.png"
  brand_color: "#0F766E"
  default_prompt: "使用 $workflows-evidence-diagnostics 分析这个反复失败或状态不一致的问题，并根据证据确定下一步。"

policy:
  allow_implicit_invocation: false

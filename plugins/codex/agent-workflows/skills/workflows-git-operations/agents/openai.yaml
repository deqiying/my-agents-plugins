interface:
  display_name: "Workflow Git Operations"
  short_description: "Disciplined Git operations and identity errors"
  icon_small: "./assets/icon.png"
  icon_large: "./assets/icon.png"
  brand_color: "#16A34A"
  default_prompt: "Use $workflows-git-operations to initialize repositories, perform scoped Git operations, handle Git author identity errors without unapproved global config, write Chinese commit messages, handle safe locks, and enforce approval gates."

policy:
  allow_implicit_invocation: true
